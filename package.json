{
  "name": "tones",
  "version": "1.0.0",
  "description": "Play a virtual piano.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "tsc": "tsc",
    "browserify": "browserify ./dist/main.js -o ./public/assets/js/tones.js",
    "cleanup": "rm -rd ./dist",
    "build": "npm run tsc && npm run browserify && npm run cleanup"
  },
  "keywords": [
    "piano"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
